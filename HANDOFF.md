# HANDOFF — B-183·B-184·B-185 소형 버그·무결성 배치 (2026-08-08)

> **로테이션 규칙**(B-120, 2026-07-19): 최신 3개만 유지, 새 엔트리
> 추가 시 초과분 절삭 — 과거는 git 이력·HISTORY.md 참조.

## 최신 작업: 감사 P2 3건 처리(3커밋)

```
e6dd30d fix: 라이트박스 720px 상한 적용 — 선택자 특이도 정렬 (B-183)
cf0b057 fix: 저장 띠 분기를 앱 표준 경계 899.98px로 통일 (B-184)
03f465d fix: applyGuards 배열·항목 가드 + inboxId 보존 (B-185)
2c7f3ca docs: HISTORY 갱신
```

커맨드센터 지시서(`DISPATCH.md`), 근거 `audit-2026-08-01.md` P2-04/02/06.
착수·커밋 전 `git status` 클린 확인. **감사 근거 실코드 대조 결과: 3건 다
현존**(줄 번호만 이동 — P2-04는 1094/1345→1226/1499, 그 외 내용 일치).

## B-183 — 라이트박스 720px (`e6dd30d`, style.css)

`.sc-lightbox-box`(클래스 1개)가 `.modal .box{max-width:520px}`(클래스 2개)에
특이도로 밀려 있었다. `!important`·ID로 올리지 않고 **선택자 형태를 이긴 쪽과
같게** 맞춤(`.modal .sc-lightbox-box`) — 동점이면 뒤에 선언된 쪽이 이긴다.

**수용 기준**: ✅ 890/1280/1440px 박스 폭 520→**720px** 실측 / ✅ 모바일 390px
350px 그대로(뷰포트가 작아 상한 무관) / ✅ 좌우 넘김 1/3→2/3→3/3→순환·1장일 때
화살표 숨김·닫기 버튼·ESC 회귀 0, 콘솔 에러 0.

## B-184 — 저장 띠 중복 노출 (`cf0b057`, style.css)

저장 띠 숨김이 `max-width:760px`, 하단 앱바는 `max-width:899.98px`이라
761~899.98px에서 둘이 동시에 떴다. **그 블록 전체를 옮기지 않고 저장 띠 2줄만**
표준 경계 블록으로 이동 — 760px 블록의 나머지는 소형 화면 타이포·간격 전용이라
경계를 통째로 옮기면 광범위한 시각 변화(B-25 영역, ❄️동결)가 생긴다.

**노출 매트릭스**(실측):

| 폭 | 저장 띠 | 하단 앱바 | 동시 노출 |
|---|---|---|---|
| 390 / 760 | ✕ | ○ | 없음 |
| **761 / 830 / 899** | ✕ *(수정 전 ○)* | ○ | **없음** *(수정 전 있음)* |
| 900 / 1280 | ○ | ✕ | 없음 |

**수용 기준**: ✅ 전 구간 동시 노출 0, 각 구간 의도된 쪽만 노출.
✅ B-172 치명적 안내(`.is-err`) 예외는 761~899 포함 전 구간에서 그대로 노출됨을
별도 실증(이게 깨지면 로드 완전 실패 안내가 모바일에서 사라진다).
※ 감사 표현은 "겹침"이었으나 빈 상태 실측에서 기하학적 겹침 픽셀은 0이었다 —
확정된 결함은 **동시 노출**(B-172가 없애려던 중복 안내)이고 수용 기준도 그것.

## B-185 — applyGuards 커버리지 (`03f465d`, state.js)

① **배열 자체 가드**: `chatHistory`·`regNews`·`savedRoutes`·`prep`·`steps`가
`||[]`라 truthy 비배열을 통과시켰다 → 전부 `guardArr`. `prep`/`steps`는 이관
블록이 `.forEach`로 **먼저** 읽으므로 가드를 그 앞에 배치.
② **항목 가드** `guardItems()` 신설 — 배열 안에 `null`·문자열이 섞이면 이어지는
`.map`에서 속성 접근만으로 TypeError가 나 applyGuards 전체가 멈추고 화면이 빈
채로 선다(guardArr와 같은 단일 실패점). 지시서대로 **정규화가 아니라 폐기** +
경고 1줄. complexes·listings·scraps·actions·assets.items 5곳.
③ **inboxId 보존**: 스크랩 가드가 객체를 통째로 새로 만들어 거기 없는 필드는
로드마다 조용히 사라졌다. 문자열일 때만 보존 + JSDoc 등재.

**지시서 밖 발견 1건(수정함)**: B-30 `assignee`/`due` 보정이 prep 이관 블록보다
**앞**에 있어서, 이관으로 새로 생긴 액션만 그 필드 없이 저장됐다가 "다음
로드"에서야 채워졌다 — 같은 state를 두 번 통과시키면 결과가 달라지는 상태라
수용 기준의 왕복 diff 0을 구조적으로 위반한다(실제로 첫 검증에서 actions 불일치로
잡힘). 이관 뒤로 옮겨 한 번에 수렴시킴.

**수용 기준**: ✅ 오염 백업(비배열 5필드 + null·문자열·숫자 항목) 로드 →
**크래시 0**, 정상 항목 전부 보존(단지·매물·스크랩·액션·자산 각 1건), 폐기 경고
15건, 화면 정상 렌더 / ✅ 정상 state 왕복 diff **0**(수정 전엔 actions 불일치) /
✅ inboxId 왕복 보존 실증 — 저장 JSON에도 남고 재로드 후에도 유지, 직접 담은
스크랩은 `undefined` 그대로 / ✅ pullInbox 중복 방지 회귀 — 이미 담긴 `abc123`
재수신 시 중복 0(1건 유지), 신규 `new999`만 추가 / ✅ 로그인·게스트 양 경로
5탭 전환 회귀 0, 콘솔 에러 0.

## 잔여·확인 필요
- **이번 배치는 사용자 실기기 확인이 사실상 불필요** — B-183은 넓은 화면(PC)
  라이트박스, B-184는 태블릿 폭 구간, B-185는 데이터 방어라 iPhone 상시 사용
  경로에서 눈에 띄는 변화가 없다. 굳이 본다면 **PC에서 수집함 사진을 크게 열었을
  때 예전보다 커졌는지**(B-183) 정도.
- **직전 배포분(8/8, origin 0978353) iPhone 확인 6건은 그대로 유효** — 인스타
  임베드 렌더·릴스 재생·임베드 위 스크롤·"인스타에서 보기" 새 탭·비공개 게시물
  안내·매물 지도 직행.
- **B-163 관찰 계속**: 핀치줌 팬 의심(재현 조건 미확정).
- 기존 Safari 관찰 목록(audit sticky/`:has()` 2건 + B-182 시나리오 5건) 변동 없음.
- **미push** — 배포 시점은 사용자 결정.
- 참고: `.git/index.lock` 진범은 커맨드센터 git 조회로 규명됨(8/8 검증 리포트).
  이번 세션은 손 A도 `--no-optional-locks`로 조회해 lock 재발 0.

---

# 이전 핸드오프 — B-198 인스타 인앱 임베드 + B-195 charset (2026-08-08)

> **로테이션 규칙**(B-120, 2026-07-19): 최신 3개만 유지, 새 엔트리
> 추가 시 초과분 절삭 — 과거는 git 이력·HISTORY.md 참조.

## 최신 작업: 인스타 임베드 정식 구현(2커밋)

```
8d0d10f fix: index.html <meta charset="utf-8"> 추가 (B-195)
af233ef feat: 수집함 인스타 인앱 임베드 (B-198)
d5c3d1f docs: HISTORY 갱신
```

커맨드센터 지시서(`DISPATCH.md`), 손 A 단독. 착수·커밋 전 `git status` 클린 확인.
B-196 스파이크 판정을 그대로 따랐고, 스파이크가 못 정한 것 1건(sandbox)은
직접 실측해 결정했다.

## B-198 구현 요지

**URL 정규화**(`scIgShortcode`/`scIgEmbedUrl`): raw에서 숏코드만 뽑아
(`[A-Za-z0-9_-]`로 엄격 제한) `https://www.instagram.com/p/{code}/embed/captioned/`를
**고정 템플릿으로 재조립**. 사용자 입력 문자열을 URL에 그대로 넣지 않는 게 요점.

**추가폼**: 기존 "내용을 가져올 수 없어요" 안내 분기 2곳(폴백 input·Tiptap
onUpdate)을 `scSyncIgEmbed` 한 줄로 대체 — 감지·제거 양방향. 같은 숏코드면
재생성하지 않아 타이핑 중 iframe이 매 글자 재로드되지 않는다.

**수정 모달**: `sem_igEmbed` 컨테이너 신설(index.html) + `openScEdit`에서 렌더.
**노션 대체의 본체가 이쪽** — 담긴 카드를 열면 내용이 그대로 보인다.
(지시서 스코프대로 "열 때 렌더"다. 모달 안에서 링크를 새로 붙여넣는 중에는
실시간 갱신되지 않는다 — 필요하면 후속 발급.)

**높이**: 인스타가 보내는 `postMessage MEASURE`를 받아 맞춘다(공식 embed.js
미로드 = 외부 스크립트 추가 0). **origin 완전 일치 + 발신 창이 우리 iframe인지
`contentWindow` 대조**를 둘 다 통과할 때만 반영하고 값도 유한 양수 검사.
600px 상한 초과분은 명시적 "펼치기" 버튼.

**sandbox — 실측 후 채택**(지시서가 판단을 맡긴 항목):
`allow-scripts`만 주면 프레임 origin이 `"null"`이 돼 위 origin 검증이 막히고
높이 자동조절이 죽는 것을 4조합 실측으로 확인. 채택한
`allow-scripts allow-same-origin allow-popups`는 렌더·캡션·높이 전부 정상이면서
**상위 프레임 이동(앱을 다른 데로 끌고 가기)·폼 제출·다운로드·모달이 기본
차단**으로 남는다. 교차 출처라 same-origin이 우리 문서 접근을 열어주지 않고
sandbox 자가 해제 우려도 해당 없음. popups는 "인스타에서 보기" 링크를 살리려 허용.

**정리 훅**: 닫힌 모달에 iframe이 남으면 계속 로드·재생되므로 `closeModal`
(utils.js)에 typeof 가드 1줄. 닫기 경로가 버튼 4곳 + **ESC**로 흩어져 있어
(ESC는 utils가 `closeModal`을 직접 호출하므로 버튼 핸들러만 고치면 샌다)
한 곳에 모았다.

state 스키마·`api/preview.js`·`scPreviewSkipDomain` 무접촉. 임베드는 저장하지
않는 렌더 시점 파생물.

## 수용 기준 7항 결과 — 전부 통과

1. **URL 5형태** ✅ `/p/`·`/reel/`·`/reels/`·`/{user}/p/`·쿼리(igsh) 전부
   정규화 후 렌더, iframe 안 캡션 실확인(`leomessi CAMPEONES DEL MUNDO…`)+미디어.
   `/tv/`도 커버. 코드 없는 프로필 URL·비인스타·평문은 `null`(임베드 안 만듦).
   주입 시도(`/p/AB"><script>…`)는 `AB`에서 잘려 무해.
2. **추가폼 양방향** ✅ 비인스타 → 미노출 / 인스타 → 임베드 / 링크 삭제 →
   미노출. 같은 글 재타이핑 시 iframe src 동일(재생성 0).
3. **수정 모달** ✅ 인스타 담긴 카드 → 임베드+캡션, 없는 카드 → 컨테이너 미노출.
4. **높이·상한·펼치기** ✅ MEASURE 수신으로 fullH 928(추가폼)/933(모달),
   표시 높이는 600 고정 → 펼치기 928 → 접기 600, 버튼 문구도 전환.
   origin 검증 코드 실재(+sandbox 하에서도 통과 실측).
5. **콘솔 에러 0 / 회귀 0** ✅ 4개 시나리오 전부 에러 0. 폼 열기·초기화·
   다른 모달(프로필) 열고 닫기 정상. 모달 닫은 뒤 잔존 iframe 0(버튼·ESC 양쪽).
6. **charset** ✅ `python3 -m http.server`(charset 무헤더)로 열어도 탭 라벨
   "대시보드/자산/매물/액션/수집함" 정상, `document.characterSet=UTF-8`,
   SyntaxError 0. 바이트 오프셋 33(1024 한도 안, 첫 비ASCII보다 앞).
7. **실기기 항목** → 아래.

**검증 하네스 주의**: Tiptap `setContent()`는 기본이 `emitUpdate:false`라
두 번째 인자를 주지 않으면 `onUpdate`가 안 불려 "감지 실패"로 **오탐**이 난다
(첫 실행이 이걸로 FAIL 났고 코드가 아니라 하네스 문제였음). 다음 검증자 참고.

## 잔여·확인 필요
- **사용자 iPhone Safari 확인**(육안·터치 판정은 사용자 몫):
  1. **B-198 임베드 렌더** — 인스타 링크를 담고 카드를 열었을 때 사진·캡션이
     실제로 보이는지. iOS의 트래킹 방지(ITP)가 서드파티 iframe에 영향을 주는지는
     **실기기에서만 확정 가능**(쿠키 0 환경에서도 렌더된 것은 확인).
  2. **릴스 영상 재생** — 탭했을 때 재생되는지, 소리 정책.
  3. **스크롤 간섭** — 임베드 위에서 손가락을 움직일 때 페이지 스크롤이
     먹히는지(iframe이 터치를 삼키는지). B-163 핀치줌 관찰과 겹칠 수 있음.
  4. **"인스타에서 보기" 링크** — 새 탭으로 열리는지(sandbox allow-popups 관련).
  5. 비공개 게시물을 담았을 때 인스타 자체 안내가 프레임 안에 뜨는지
     (스파이크에서 비공개 URL을 못 구해 미검증으로 남은 항목).
- **B-195는 배포 후 자연 확인** — Vercel은 원래 헤더가 있어 무증상이었으므로
  실사용 변화 없음이 정상.
- 직전 배포분(B-197) 확인 항목 유효: 앱 새로 열면 매물(지도) 직행인지.
- **B-163 관찰 계속**: 핀치줌 팬 의심(재현 조건 미확정).
- 기존 Safari 관찰 목록(audit sticky/`:has()` 2건 + B-182 시나리오 5건) 변동 없음.
- **미push** — 배포 시점은 사용자 결정.

---

# 이전 핸드오프 — B-196 스파이크 + B-197 완료 (2026-08-08)

> **로테이션 규칙**(B-120, 2026-07-19): 최신 3개만 유지, 새 엔트리
> 추가 시 초과분 절삭 — 과거는 git 이력·HISTORY.md 참조.

## 최신 작업: 인스타 임베드 스파이크(커밋 0) + 모바일 첫 화면 직행(커밋 1)

```
e56c854 feat: 모바일 첫 화면 매물 탭(지도) 직행 (B-197)
13e2b30 docs: HISTORY 갱신 — B-196 스파이크·B-197
```

커맨드센터 지시서(`DISPATCH.md`), 손 A 단독 순차.
착수 전 `git status` 클린 확인(추적 파일 미커밋 변경 0).
⚠️ 착수 시 `.git/index.lock`이 **8/7 15:22자 0바이트로 잔존**해 있었다(직전
세션 git 프로세스가 비정상 종료). 살아있는 git 프로세스가 없음을 확인하고
제거 후 진행 — 다른 에이전트 작업물 손상 없음.

---

## B-196 스파이크 — 판정 **가능**, 정식 발급 권장 (커밋 0)

리포트 `spike-2026-08-08-B196.md`, 증거 `_spike-B196/`(스크린샷 4 + results.json).
`api/preview.js`·`state`·기존 코드 **무접촉**.

**결론**: `instagram.com/p/{code}/embed/captioned/`를 클라이언트 iframe으로
직접 그리면 **로그인 없이 사진·릴스 영상·캡션 전문이 렌더된다.** 서버를 안
거치므로 크롤링 차단(⛔ 기결정)과 무관 → **노션 이중 기록 제거 가능**.

**최대 함정**: `curl` 기본 요청엔 `x-frame-options: DENY`가 찍힌다. 하지만
응답에 `vary: Sec-Fetch-Site, Sec-Fetch-Mode`가 있어, 실제 iframe 형태
(`Sec-Fetch-Dest: iframe`)로 요청하면 **XFO가 사라진다.** 헤더만 보고
"불가"로 종결했으면 오판할 뻔했다.

**실측**: 게시물 5개 × 변형 2개 × 프로필 2개(데스크톱·iPhone 13 에뮬).
로그인 벽 0건·429 0건, 같은 글 반복 10회 전부 200/렌더 성공, 쿠키 0
콜드 컨텍스트도 정상, 없는 숏코드는 "링크 잘못됨" 카드로 안전 degrade.

**꼭 지킬 것 2가지**
- `/embed/`가 아니라 **`/embed/captioned/`** — 전자는 캡션이 빠진다(806자 vs 165자).
- **URL 정규화 필수** — `/reels/{code}/`·`/{username}/p/{code}/`는 프레임이
  아예 안 뜬다. 숏코드만 뽑아 `/p/{code}/`로 재조립하면 5형태 전부 통과
  (릴스 숏코드도 `/p/`로 넣으면 정상, 아이폰 공유링크 `?igsh=`도 무해).

**보너스**: 인스타가 `postMessage`로 `MEASURE{height}`를 보내와, 외부
`embed.js` 로드 없이 iframe 높이 자동조절 가능(보안 접점 추가 0).

**붙일 자리**: 이미 인스타를 감지해 "내용을 가져올 수 없어요" 안내를 띄우는
`scraps-form.js:122`(폴백)·`:215`(Tiptap)의 `#sc_ogPreview` 분기 — 그 카드를
임베드로 교체. 카드 목록엔 넣지 말 것(스크롤 시 iframe 수십 개 동시 로드).
현재 앱엔 CSP·서비스워커가 전혀 없어 `frame-src` 차단 위험 0.

**커맨드센터 판단 요청 1건**: 캡션이 길면 970px까지 커진다 — 높이 상한+
"더 보기"를 둘지(접기 지양 정책과 충돌 소지).

---

## B-197 — 모바일 첫 화면 = 매물 탭(지도) 직행 (커밋 1개, `e56c854`)

`js/boot.js` 단일 파일. 모바일(`MOBILE_APP_MQ`, ≤899.98px)만 첫 진입을
매물 탭으로, **데스크톱은 대시보드 무변경**.

**지시서와 달라진 점(보고)**: 지시서는 `js/nav.js` 예상이었으나 실제 초기
패널 결정은 `boot.js`의 `restoreLastView()`(B-24 세션 복원)가 하고 있었다.
그리고 거기에 **함정이 있었다** — 이 함수는 boot 체인에서 무조건 실행되는데,
미로그인 401 경로는 `load()`가 `renderAll()` **전에** 빠져나가 `state`가
`null`이다. 그 상태로 매물 탭을 열면 `refreshOverview()`가
`state.complexes`를 읽다 깨진다(지시서가 예상 못 한 경로).
그래서 `applyInitialPanel()`로 개명하고 **`renderAll()` 끝에서 1회만**
실행하도록 옮겼다 — state가 보장되고, 로그인·게스트 진입 후에도 첫 화면이
제대로 매물로 잡힌다(단순히 boot 체인에 뒀으면 로그인 후엔 대시보드에
떨어졌을 것).

**우선순위**: B-24 세션 복원이 있으면 그쪽이 우선(이번 세션에 보던 화면
유지는 종전대로), 복원이 없을 때 = 앱을 새로 연 첫 화면에만 B-197 적용.

**검증**(Playwright, iPhone 13 에뮬 + 1440px 데스크톱, `/api/state` 목업)
① 모바일 콜드 로드 → `activePanel=props`·`.panel.on=panel-props`·탭 `data-on`
   props·지도 실크기 **390×541**(0×0 아님 = B-169 계열 사이징 이슈 없음)
② 탭 전환 5회(scraps→dash→props→assets→props) 전부 정상, 매물 복귀 때마다
   지도 390×541 재계산
③ 데스크톱 콜드 로드 → `dash` 유지, 콘솔 에러 0 (무변경 확인)
④ 새로고침 4회 연속 전부 `props` / 자산 탭 보다가 새로고침 → `assets` 복원
   (B-24 우선순위 정상)
⑤ 미로그인 401 콜드 로드 → `dash` 유지·**크래시 0**(위 함정 가드 실증) /
   게스트 모드 모바일 → `props`+지도 390×540, 게스트 데스크톱 → `dash`
⑥ 경계값 899px → `props`, 900px → `dash` (분기 정확)
⑦ B-149 공존 — `sh_propViewMode=list` → 매물 탭+리스트뷰, `=map` → 지도
   390×541. 첫 화면이 바뀌어도 뷰 모드 복원 그대로 작동

**검증 하네스 주의(다음 사람용)**: `python3 -m http.server`로 이 앱을 띄우면
charset 헤더가 없어 JS의 한글이 깨져 `SyntaxError`로 앱이 아예 안 뜬다
(첫 실행이 이 오염으로 전부 오탐 → 폐기하고 재실행함). **이게 곧 B-195의
실증** — `index.html`에 `<meta charset="utf-8">`이 없어서다. 검증 서버는
`Content-Type: ...; charset=utf-8`을 반드시 붙일 것.

## 잔여·확인 필요
- **B-196은 커맨드센터 발급 대기** — 스파이크 판정만 했고 구현 코드 0.
  높이 상한 정책 결정 1건 필요(위 참조).
- **사용자 iPhone 실기기 확인**:
  1. **B-197** — 앱을 새로 열었을 때 바로 매물(지도) 화면인지, 지도가
     잘리거나 회색으로 뜨지 않는지. 탭 옮겼다 돌아와도 정상인지.
  2. **B-196 선확인(발급 전 참고용)** — 실제 iOS Safari에서 인스타 임베드가
     뜨는지는 실기기에서만 확정 가능(ITP 영향). 렌더 자체는 쿠키 0에서도
     됐으므로 통과 예상이나 단정하지 않음.
  3. 직전 배포분(B-193R·B-194R) 확인 항목 **아직 유효**: 지도 핀 탭 깜빡임
     소멸 / 사진 첨부 직후 저장 시 "사진 처리 중…" 후 첨부 사진이 썸네일로 /
     HEIC 실패 시 "사진을 읽지 못했어요" 안내.
- **B-195**(charset) 이번에 실증됨 — `index.html` 접촉 시 1줄 동반 권장.
- **B-163 관찰 계속**: 핀치줌 팬 의심(재현 조건 미확정).
- 기존 Safari 관찰 목록(audit sticky/`:has()` 2건 + B-182 시나리오 5건)
  변동 없음.
- **미push** — 배포 시점은 사용자 결정(실기기 확인은 배포 후에만 가능).

---
