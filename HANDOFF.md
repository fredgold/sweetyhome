# HANDOFF — B-57 전 탭 반응형 너비 통일 (2026-07-13)

## 1. 목표
`BACKLOG.md`의 B-57 처리. 문서형 탭(대시·자산·액션·수집함)도 매물탭과
동일하게 대형 모니터(>1440px)에서 전체폭이 되게 함. B-51에서 "문서형 탭은
가독성 위해 1440px 유지"로 정했던 결정을 이번 지시서로 의도적으로 반전.

## 2. 완료
**커밋 완료.** (push는 아직 — 아래 참고)

```
2b79d16 style: B-57 전 탭 반응형 너비 통일
```

- **수정 파일**: `style.css` 단 1개, 라인 387~395(셀렉터 1줄 변경 + 주석
  정정)만. JS·스키마 무접촉.
- **수정 내용**: `body:has(#panel-props.on) .wrap{max-width:none;}` →
  `.wrap{max-width:none;}`(매물탭 전용 프리픽스 제거, 데스크톱
  `@media (min-width:900px)` 블록 내부는 그대로).
- **건드리지 않은 것**: `line 386`
  `body:has(#panel-props.on) .wrap{padding-bottom:0;}`는 지시대로 그대로
  유지 — 매물탭만 뷰포트 고정 flex라 하단여백 0이 맞고, 다른 탭은 24px
  필요하기 때문. 자산 테이블 초광폭 상한 조정도 지시대로 미포함.
- **주석 정정**: B-51 주석이 "문서형 탭 1440 유지" 서술이라 의미가
  반대가 됐던 것을 "전 탭 대형 모니터 전체폭(B-57로 스코프 해제)"로 정정.
- **검증**(Playwright, 로컬 정적 서버+게스트모드):
  - 2560px: 대시/자산/액션/수집함/매물 5개 탭 전부 `.wrap` 너비 2560px
    (전체폭) 확인.
  - `padding-bottom`: 매물탭만 `0px`, 나머지 4개 탭은 전부 `24px`로
    정확히 스코프 유지 확인(line 386 무회귀).
  - 자산탭 2560px 스크린샷 육안 확인: 테이블·요약카드가 전체폭으로
    자연스럽게 확장, 깨짐·오버플로 없음.
  - 1400px(구 1440 상한 이하)·390px(모바일): 5개 탭 전부 무회귀(이번
    변경 전후 결과 동일).
  - CSS 중괄호 균형·`git diff --check` 통과.
  - 임시 설치한 `playwright`(`npm install --no-save`)와 검증용 임시
    파일·서버는 세션 종료 전 전부 삭제. `package.json` 변경 없음.

## 3. 미완 / 다음 단계
- **push 아직 안 함** — 이번 지시서엔 push 여부가 명시되지 않았고, 직전
  세션들에서 확립된 "기능+문서 커밋 후 push" 관행을 따르되, 사용자가
  이 문서를 보는 시점에 원하면 바로 push 가능(코드는 이미 로컬에 완결
  커밋됨).
- 자산 테이블 초광폭(2560/4K) 가독성 문제가 실사용 중 발견되면 별도
  백로그로 등록해 상한 재조정 검토(지시서에서 의도적으로 이번 범위 밖).
- BACKLOG.md 기준 "매물 영역 외 전면 파킹, B-57만 유일 예외"였던 상태 —
  B-57 완료로 이제 문서형 탭 관련 파킹 예외가 소진됨. 다음 비-매물 작업은
  커맨드센터의 새 지시 필요(매물 영역 항목은 계속 착수 가능).

## 4. 주의점
- PIN·API 키·실제 금액 데이터는 이 문서 어디에도 기록 안 함.
- **BACKLOG.md는 커맨드센터 소유(읽기 전용)** — 이번 세션에서도 전혀 수정·커밋 안 함.
- `js/nav.js` 무접촉 원칙은 B-56에서의 일회성 예외 이후 다시 기본으로
  복귀 — 이번 B-57은 `style.css`만 건드림, nav.js 무관.

## 5. 컨텍스트
- 이 레포는 "머리(커맨드센터, 코드 미수정) — 손 A(Claude Code, 나) — 손 B(Codex)" 3자 협업
  구조. SSOT는 `CLAUDE.md`, 실행 규칙은 `AGENTS.md`, 이력은 `HISTORY.md`, 백로그는
  `BACKLOG.md`(커맨드센터 전용).
- 직전 세션들: v5 단지·매물 2계층 전환(E-01) → B-12 관련 다수 작업 → ... →
  B-28 → B-18 → B-38 → B-39(`d943fd5`) → B-30(`4ee58cd`) →
  B-56(`21cb4b9`, Codex 지시서를 손 A가 사용자 승인 하에 대신 실행) →
  **이번 세션(B-57, `2b79d16`)**.
