# HANDOFF — B-176~B-181 완료, B-182 계획 승인 대기 (2026-08-02) 감사2회차 후속

> **로테이션 규칙**(B-120, 2026-07-19): 최신 3개만 유지, 새 엔트리
> 추가 시 초과분 절삭 — 과거는 git 이력·HISTORY.md 참조.

## 최신 작업: backdrop 스크롤잠금·esc()일관화·900px탭깨짐·AA대비·안전체크상시노출·터치타깃38px 일괄(3커밋)

```
88165cf fix: 모달 backdrop 닫기 시 body 스크롤 잠금 잔류 (B-176)
c9dd145 fix: 동적 속성 삽입 esc() 일관화 (B-177)
c3d19d0 fix: 900px 정확 지점 상단 탭 세로 파손 (B-178)
caf224a fix: 상태 텍스트 5종 AA 대비 미달 수정 (B-179)
fe589c7 feat: 전세 안전체크 9필드 데스크톱 상시 노출 전환 (B-180)
874e7d3 fix: 모바일 터치 타깃 38px 일괄 ①파괴·정밀 조작 우선 (B-181-1)
2d9bf68 fix: 모바일 터치 타깃 38px 일괄 ②대시보드·자산·액션나머지·로그인 (B-181-2)
a82f733 style: 터치 타깃 38px 확대 — 수집함·매물·모달 나머지 (B-181-3)
```

커맨드센터 지시서(`dispatch-2026-08-02-B176-B182.md`)로 착수. 근거는
`audit-2026-08-01.md`(감사 2회차). 손 A 단독, B-176→...→B-181 순서
엄수(한 항목=한 커밋, B-181만 영역별 분할 허용). 착수/커밋 전후
`git status`로 손 B(B-160) 미확정 변경 확인 — 이번 세션 전 구간에서
충돌 없었음(작업 내내 클린).

**B-176**(`js/properties.js` 1줄): 공용 backdrop 핸들러가
`m.classList.remove('open')`만 호출해 `unlockBodyScroll()` 미호출 →
lock count 잔류 → `body.style.position:fixed` 영구 고착. `closeModal(m.id)`
경유로 교체(`complexDetailModal`의 `closeListingDetail()` 분기는 유지).
`classList.remove('open')` 직접 호출 전수 grep — 모달 아닌 `.open`
사용처(폼시트 등)는 무접촉 확인. 검증: backdrop 닫기 후
`body.style.position===''`, ESC 중첩(B-127)·라이트박스 backdrop·
모달 2개 연속 열기/닫기 lock count 회귀 없음.

**B-177**(`assets.js`+`nav.js`+`actions.js`+`scraps-render.js`+
`properties.js`+`profile.js`): 백업 임포트로 유입되는 `id`가 다수
`data-*` 속성에 비이스케이프 삽입되던 것 전수 `esc()` 적용(리스트뷰
vs 갤러리뷰 불일치가 실증 사례). `properties.js`의 `CSS.escape(lid)`
4곳은 전부 `querySelector()` CSS셀렉터 컨텍스트라 정상 사용 확인(false
alarm, 무수정). 오염 ID(`sc7" data-audit-x="1`) 합성 주입 검증 —
임의 속성 미생성, `data-scid` 원문 보존, 수정/삭제 핸들러 정상.
**applyGuards ID 형식 제한은 계획만**(코딩 안 함) — 기존 백업 비정형
ID 마이그레이션 매핑, `complexId` 등 관계키 참조무결성, 정규식 밖
ID 처리 정책 3가지가 걸려있어 별도 지시 필요.

**B-178**(`style.css` 1줄): `@media(min-width:900px)`에서 `.atab`에
`white-space:nowrap`+`flex-shrink:0` 추가 — 정확히 900px 지점에서
"대시보드" 등이 한 글자씩 세로줄바꿈되던 것 해소. 899(하단앱바
58px)/900/901/1440(상단탭 40px) 4점 실측 — 900px 탭 높이 40px대,
세로줄바꿈 0, 로고·동기화칩 밀림 없음.

**B-179**(`style.css` 5개 색상값): 전경색만 진하게(배경·hue 유지,
B-129/130 선례) — 액션 마감초과 `#B16A63`→`#9B554E`, 수집함 매매
`#6B7C93`→`#5F6F83`, 제외 `#B5602F`→`#A6582B`, 메모 `#7A7A50`→
`#73734B`, `.chip.ok` `#3C7A4C`→`#3B784B`. 5종 전부 4.5:1 이상
확보, `--sc-*` 토큰 공유 소비처 전수 재검산 완료(개선만, 악화 없음).

**B-180**(`js/properties.js`+`style.css`): `safetySectionHTML()`의
인라인 `style="display:none"`을 클래스+미디어쿼리로 전환 —
데스크톱(≥900px) 9필드 상시노출, 모바일 접기 유지(B-147b 반응형
예외 패턴). **함정 발견·수정**: `.safety-list` 클래스가 읽기전용
패널(`safetyReadOnlyHTML()`)과 공유돼 있어 바레 셀렉터로 숨기면
읽기전용 패널까지 깨짐 — `.safety-wrap .safety-list` 자손 셀렉터로
스코프. 데스크톱 즉시노출·입력동작, 모바일 접기/펼치기, safety 상태
저장 무변경 확인.

**B-181**(`style.css`, 3커밋 분할): audit §3.14 터치 타깃 38px
미달 표 전체 — 시각 크기 유지 + pseudo hit-area(`::after`)/
min-height(B-136 패턴). ①파괴·정밀 우선(액션 수정·별·삭제,
마일스톤삭제, 안전체크토글) ②대시보드·자산·액션나머지·로그인게스트
③수집함(검색/정렬/뷰토글/필터칩/카드액션/원문토글)·매물(정렬칩/
더보기/필터/tri-state/입력)·모달(라이트박스·footer버튼). **구조적
블로커 2건 해결**: `.sc-view-toggle`·`.tri-seg` 둘 다 `overflow:hidden`
컨테이너라 `::after` 확장이 부모에 잘렸음 — overflow:hidden을 빼고
`:first-child`/`:last-child`에 개별 모서리 radius를 줘서 같은 시각을
유지하며 해제, 세그먼트 사이 0px 인접이라 바깥쪽 모서리만 확장(가운데
겹침 방지). gap 기반 항목(필터칩·카드액션·정렬칩 등)은 gap의 절반만
확장해 형제와 안 겹치게 — 이 경우들은 38px에 못 미쳐도 구조적 최대치로
문서화(B-136/①의 전례). Playwright 390×844 실측: 각 항목 effective
hit area, 인접 겹침 0, 데스크톱(1440) `::after content:none` 무변경,
뷰토글·tri-state 클릭 기능 정상.

- **B-176~B-181 완료·push 완료**(`88165cf`~`a82f733`, 위 8커밋).
- **B-182[P1·게이트] 모달 포커스 격리·키보드 접근 — 계획만 보고, 코딩
  미착수, 승인 대기**:
  - 대상: `.modal` 9개(export/import/scEdit/scLightbox/scImport/
    assetAi/profile/propImport/complexDetail, 전부 openModal/
    closeModal 공용) + 로그인 overlay(별도 메커니즘, 소규모 병행).
    폼시트·⋯더보기 메뉴는 audit 재현 목록 밖이라 제외.
  - 방식: `inert` 우선(모달 열림 시 형제 컨텐츠에 부여, 배경 tab/
    click/스크린리더 전부 차단) + keydown Tab순환 폴백 이중화(B-127
    slash-menu와 동일 계층). openModal/closeModal 중앙화로 개별
    모달 수정 최소화, 트리거 엘리먼트 전달용 옵셔널 파라미터 추가.
  - 회귀검증계획: B-127 ESC 중첩 순서, 중첩모달(단지상세>라이트박스)
    inert 계층, 폼시트/⋯메뉴 비대상 확인, Tab/Shift+Tab 순환, 초기
    포커스·트리거 복귀, 데스크톱/모바일 각 진입경로.
  - 부수(UX-03, 별도 트랙 제안): 로그인 게스트 div·필터칩 span·매물행
    — 포인터전용 컨트롤 키보드화. 메커니즘이 달라 리스크 낮아 트랩
    작업과 분리 가능.
  - audit이 "위험도 높음"(모든 close 경로 단일화와 겹침)으로 표시한
    항목이라 승인 후 착수.
- **Safari 실기기 확인 필요**: 이번 배치(B-176~181)는 전부 Chromium
  computed-style 정밀 측정으로 검증, 새로 추가된 Safari 전용 우려
  없음. 기존 audit 플래그 2건(sticky/`:has()`)은 그대로 유효.
- **B-164~171잔여**(이전 세션들, 아직 미확인): 매물 메모 행간·별점/
  즐겨찾기 별 탭 감각. 대부분은 후속 세션에서 이미 처리됨(B-166~174).
- **B-163 관찰 계속**: 핀치줌 팬 의심(재현 조건 미확정), 이번
  지시서 범위 밖.

---

# 이전 핸드오프 — B-173+B-172+B-174 완료 (2026-08-01) 자산금액정렬·저장문구숨김·수집함다중선택

> **로테이션 규칙**(B-120, 2026-07-19): 최신 3개만 유지, 새 엔트리
> 추가 시 초과분 절삭 — 과거는 git 이력·HISTORY.md 참조.

## 최신 작업: 자산 요약행 금액 정렬 + "자동 저장됩니다" 모바일 숨김(에러 예외) + 수집함 다중선택·일괄삭제

```
76a93dc fix: 자산 요약행 금액 정렬 — .reg-summary-main flex:1 누락 (B-173)
5ca20b7 fix: "변경사항은 자동 저장됩니다" 모바일 숨김 — 에러는 예외 노출 (B-172)
74ed4fd feat: 수집함 다중선택 + 일괄 삭제 (B-174)
```

커맨드센터 지시서(`dispatch-2026-08-01-B172-B174.md`)로 착수. 사용자
iPhone 실사용 피드백 5차. 손 A 단독, 지시대로 B-173→B-172→B-174
순서(작은 것부터) 3커밋.

**B-173**(`style.css` 1줄): `.reg-summary`가 `justify-content:
space-between`인데 `.reg-summary-main`에 `flex:1`이 없어 여유 공간이
main/amt/caret 사이로 분배돼 이름 길이에 따라 금액 x좌표가 밀렸음.
`flex:1` 추가로 main이 여유 공간 전부 흡수. 실측 — 이름 길이 1~24자
5개 행 전부 금액 우측 edge 332.375px로 동일, 펼침(is-expanded) 회귀 0.

**B-172**(`style.css`+`js/state.js`+`js/scraps-form.js`): `#savedNote`
참조 4곳 전수 확인(지시서가 언급한 scraps-form.js 2곳 외에
state.js `load()`/`save()`도 이 요소를 씀) 결과 **충돌 발견** —
`load()`의 "클라우드·로컬 모두 불러오기 실패" 안내(동기화 칩엔 없는
유일한 문구)까지 같은 요소라 무지성 숨김 시 함께 사라짐. 사용자
확인 후 `.is-err` 클래스로 예외 노출 채택: 평소엔 모바일에서
`.saved{display:none}`, 이 치명적 안내만 `.is-err`로 노출. 텍스트를
덮어쓰는 다른 3곳(save() 양쪽 분기·수정모달 힌트/리셋)은 전부
`is-err` 제거 동반해 무관한 문구가 계속 떠 있는 누수 방지. 실측 —
정상 상태 5탭 전부 숨김, 에러 상태 노출, save() 호출 시 재차 숨김,
데스크톱 무변경.

**B-174**(`index.html`+`js/scraps-render.js`+`js/nav.js`+`style.css`):
수집함 다중선택+일괄 삭제(스코프 엄수 — 상태 일괄 변경 없음, 사용자
협의 완료). 착수 전 계획 보고 → 커맨드센터가 보완 2건 지시 후 진행.
진입 2경로(툴바 "선택" 버튼+카드 long-press 500ms, touchmove 10px로
스크롤 구분). **보완①**: 선택 모드 중 카드 내부 클릭은 캡처 리스너로
가로채지만 `<select>` 네이티브 드롭다운은 click 이전 시점에 열려
막을 수 없어 `pointer-events:none` CSS 1차 방어 추가(카드 자체만
`pointer-events:auto`로 남겨 탭이 항상 카드로 떨어지게 함). **보완②**:
선택 Set을 `renderScraps()`마다 "현재 화면에 보이는 id" 교집합으로
프루닝 — 필터/정렬 변경으로 화면에서 사라진 항목이 안 보인 채 함께
삭제되는 사고 방지(실측: 5개 중 2개 선택 후 필터 변경으로 1개만
남자 실제로도 1개만 삭제됨 확인). 삭제는 기존 개별 삭제와 동일한
filter 패턴 재사용, `confirm` 1회+`save()` 1회. 선택 상태는
`scRawViewIds`와 동일 패턴 순수 메모리(state 스키마 무접촉). 해제
트리거 3가지(취소·삭제완료·탭전환)로 한정.

**검증**(Playwright, 실 TouchEvent 시뮬레이션 포함): 목록·갤러리
각각 선택→삭제→상태 반영, 프루닝 안전성, 취소·탭전환 해제, 일반
모드 카드클릭·개별삭제 무회귀, 데스크톱 버튼 경로, long-press
실제 진입(500ms 무이동) vs touchmove 50px 스크롤 구분 전부 확인.
전체 세 항목 신규 콘솔 에러 0.

- **B-172/B-173/B-174 완료·push 완료**(`76a93dc`/`5ca20b7`/`74ed4fd`).
- **사용자 확인 요청**(Safari 실기기, 390px 근처):
  ① 자산 탭에서 이름 길이 다른 계좌들의 금액이 한 줄로 정렬되는지.
  ② 각 탭 하단에 붕 뜬 "변경사항은 자동 저장됩니다" 띠가 안 보이는지.
  ③ 수집함에서 "선택" 버튼과 카드 길게 누르기 둘 다로 다중선택
  진입되는지, long-press 중 iOS 텍스트 선택 메뉴가 안 뜨는지, 스크롤
  중엔 실수로 선택 모드에 안 들어가는지.
- **B-164~171잔여**(이전 세션들, 아직 미확인): 매물 메모 행간·별점/
  즐겨찾기 별 탭 감각·거리순 칩 권한요청·좌우 밀림 소멸·액션 전문
  표시·AI 노출 정리·지도 부분 렌더 수정. 이번 3항목과 함께 확인 가능.
- **B-163 관찰 계속**: 핀치줌 팬 의심(재현 조건 미확정), 이번
  지시서 범위 밖.

---

# 이전 핸드오프 — B-170+B-171+B-169 완료 (2026-08-01) 액션 전문표시·AI노출정리·지도 부분렌더 수정

> **로테이션 규칙**(B-120, 2026-07-19): 최신 3개만 유지, 새 엔트리
> 추가 시 초과분 절삭 — 과거는 git 이력·HISTORY.md 참조.

## 최신 작업: 액션 제목 전문 표시 + AI 노출 정리(문구 5곳) + 지도 부분 렌더 진단·수정

```
75adc57 fix: 액션 제목 모바일 전문 표시 — 말줄임 대신 줄바꿈 (B-170)
75ebe40 feat: AI 노출 UI 정리 — 문구·버튼 5곳 (B-171)
082e263 fix: 지도 부분 렌더 — visualViewport 변경 시 refresh 누락 수정 (B-169)
```

커맨드센터 지시서(`dispatch-2026-07-31-B169-B171.md`)로 착수. 사용자
iPhone 실사용 피드백 4차. 손 A 단독, 지시대로 B-170→B-171→B-169
순서(169는 진단 포함이라 마지막) 3커밋.

**B-170**(`style.css`만): `.act-text`가 모바일에서도 nowrap+ellipsis를
상속해 긴 액션 제목을 확인할 방법이 수정 버튼 진입뿐이었음. 모바일
(≤899.98px)만 `white-space:normal`로 줄바꿈 허용 — 접기·탭펼침 등
새 인터랙션 없이 그냥 다 보여줌(접기 지양 정책 그대로 적용). 기존
`align-items:center`가 늘어난 행 높이에도 체크박스·▲▼·수정/★/✕
정렬을 자동 유지(centerY 차 <0.01px 실측). B-133(`display:block`,
아이콘 흘러넘침 방지) 무회귀, 짧은 텍스트 단일행 유지, 데스크톱
무변경(nowrap+ellipsis 그대로) 확인.

**B-171**(`index.html`+`js/actions.js`): 사용자 결정(2026-07-31, AI
제안·설명 문구 불필요)에 따라 크레딧 충전 시 복원 가능하게 **로직·
핸들러는 보존, UI 노출만 제거** — `#act_suggestBtn` display:none(DOM·
onclick 핸들러 그대로), 액션/뉴스 adesc·자산채우기 자유입력 안내·
액션 빈 상태 문구에서 AI 언급 삭제(지시서 5곳 그대로). AI 채팅
패널·매물 AI평가·수집함 AI자동채움(이미 disabled+안내문구 명시)은
스코프 밖이라 무접촉 — grep "AI" 재확인으로 남은 언급이 전부 스코프
밖(AI상담 패널 자체, 자산 AI/시트 버튼 라벨, ai_log 타입명 등)임을
확인. 액션 헤더 행(.ah) 레이아웃 회귀 0.

**B-169**(`js/nav.js`, 진단 먼저 — 수정 전 원인 특정): 매물 지도뷰가
좌상단 일부만 렌더되고 위치가 튀는 버그. **원인 실측 확정** —
`scheduleMobileViewport()`가 `visualViewport` resize/scroll마다
`--app-visual-h`를 갱신하고 이 변수가 `#overviewMap`의 실제 CSS
크기를 결정하는데(Safari 동적 툴바 show/hide·백그라운드 복귀 등에서
실제 발생), `overview.refresh()` 호출이 이 경로 어디에도 없었음 —
`overview.refresh`에 스파이를 심어 직접 확인(호출 0회, 그 사이
`#overviewMap` 실측 높이는 720px→634px로 실제 리사이즈됨). 네이버
지도는 컨테이너 리사이즈를 자동 감지하지 않는 라이브러리라 내부
캔버스가 예전 크기로 굳어 부분 렌더·좌표 어긋남(위치가 충청권으로
튐)이 남. 나머지 진단 항목도 확인 — 80ms 타이밍(`switchPanel`)은
`initOverview` 자체 60ms 지연 refresh가 있어 부차적, 최근 커밋
(B-161 `51bbe68`/B-168 `131dbbf`)은 diff 전수 확인으로 배제(지도
관련 셀렉터·경로 무접촉). **수정**: `scheduleMobileViewport()`의
기존 rAF 콜백(이미 디바운스) 끝에 매물탭+지도뷰일 때만
`overview.refresh(true)` 1줄 추가. 스파이로 재검증 — 지도뷰에서는
정확히 1회, 리스트뷰·다른 탭에서는 0회(불필요한 호출 없음) 확인.
전체 탭 순회+뷰모드 토글 신규 콘솔 에러 0. 실제 지도 타일은 네이버
API 키가 배포 도메인으로 제한돼 로컬 재현 불가(기존 세션들과 동일
제약) — Safari 실기기 확인 필요.

- **B-170/B-171/B-169 완료·push 완료**(`75adc57`/`75ebe40`/`082e263`).
- **사용자 확인 요청**(Safari 실기기, 특히 스크롤로 주소창이 숨었다
  나타나는 순간 전후, 또는 백그라운드 갔다 복귀 직후):
  ① 지도가 이제 화면 전체에 정상 렌더되는지, 위치가 안 튀는지.
  ② 액션 탭에서 긴 제목이 잘리지 않고 전문이 보이는지.
  ③ AI 관련 문구·버튼이 눈에 안 띄는지(액션·뉴스·자산채우기).
- **B-164~168잔여**(직전 세션들, 아직 미확인): 매물 메모 행간·별점/
  즐겨찾기 별 탭 감각·거리순 칩 권한요청·대시보드·수집함 좌우 밀림
  소멸. 이번 3항목과 함께 한 번에 확인해도 됨(카드 위치·즐겨찾기
  별은 12:09 스크린샷에서 이미 정상 확인됨).
- **B-163 관찰 계속**: 핀치줌 팬 의심(재현 조건 미확정), 이번
  지시서 범위 밖.
