# HANDOFF — B-123 완료 (2026-07-20) 인스타 레퍼런스 강화

> **로테이션 규칙**(B-120, 2026-07-19): 최신 3개만 유지, 새 엔트리
> 추가 시 초과분 절삭 — 과거는 git 이력·HISTORY.md 참조.

## 최신 작업: 갤러리 카드 이미지 중심 강화 + 클립보드 이미지 붙여넣기 수집

```
23cbeb7 feat: 갤러리 카드 이미지 중심 강화 + 라이트박스 (B-123 ①)
fd9fbf8 feat: 클립보드 이미지 붙여넣기 수집 (B-123 ②)
```

커맨드센터 확정 스펙(일원화 방침 긍정 확인 → 2026-07-19 발급)으로
착수. 손 B 부재로 파일 락 제약 없이 진행, 지시대로 2커밋 분리.

**B-123①**(`scraps-render.js`+`index.html`+`style.css`): 갤러리 카드
중 imgs 있는 항목은 커버 이미지를 4:5 비율(`.sc-gallery-imgwrap{
aspect-ratio:4/5}`)로 확대해 인스타 피드에 가깝게, 다중 사진이면
우상단 반투명 뱃지(`⧉3`형식). 사진 없는 카드는 기존 유형색 프리뷰
완전 무변화. 이미지 클릭은 `data-sclight` 래퍼가 잡아 `stopPropagation`
으로 카드 본문 클릭(기존 편집 모달 오픈)과 버블링 분리 — 새 라이트박스
모달(`scLightboxModal`)은 기존 `.modal`/`openModal`/`closeModal`/
`data-close` 제네릭 패턴 그대로 재사용(신규 라이브러리 0), `imgs[]`
좌우 넘김은 prev/next 버튼 + 좌우 화살표 키(모달 열림 상태에서만
document 레벨로 감지) 자체 구현. 리스트뷰(피드)는 완전 무변경.

**B-123②**(`scraps-form.js`): 수집함 추가폼·편집모달 에디터(`sc_text`/
`sem_text`)에 이미지 붙여넣기 지원. 캡처 단계(`{capture:true}`)로
마운트 지점(`el`)에 리스너를 걸어 Tiptap 성공 시에도 내부 `.ProseMirror`
자체 붙여넣기 핸들러보다 항상 먼저 개입 — 클립보드에 이미지 파일이
있으면 `preventDefault`+`stopPropagation`으로 완전히 가로채
`compressImage` 경유 후 `imgs[]`에 push(기존 파일선택 업로드와 동일
썸네일 UI 재사용, `SC_MAX_IMGS` 상한 동일 적용), 텍스트가 함께 있으면
Tiptap `insertContent` 또는 폴백 `scInsertFallbackText`(기존 두 paste
핸들러가 중복 보유하던 삽입 로직을 공용 함수로 추출, 리팩터 겸함)로
에디터에 삽입 — "텍스트는 에디터로, 이미지는 첨부로" 분리 처리. **이미지가
없는 순수 텍스트 붙여넣기는 함수 맨 위에서 즉시 `return`해 기존 경로
(Tiptap 기본 처리 또는 폴백 핸들러)를 그대로 통과**시켜 무회귀 보장.

**검증**(Playwright, 로컬 Node UTF-8 정적 서버): 라이트박스 — 장수
뱃지(다중만 노출·단일 무표시) 확인, 이미지 클릭 시 라이트박스만 열리고
편집 모달은 안 열림(반대로 카드 본문 클릭은 편집 모달만 열림, 두
클릭 영역 상호 간섭 없음) 확인, prev/next 버튼·화살표 키 넘김(순환)
확인, `data-close` 버튼·백드롭 클릭 양쪽 닫기 확인, 모바일 390px
`hasTouch` 컨텍스트로 탭 열기 확인. 이미지 붙여넣기 — 이 샌드박스는
esm.sh(Tiptap CDN) 도달성이 실행마다 들쭉날쭉해(같은 스크립트 재실행
시 성공/실패가 갈림) **두 경로 모두 실측**: Tiptap 활성 시
`editor.chain().focus().insertContent()` 경로, `route().abort()`로
CDN을 의도 차단한 폴백 강제 상태에서 `scInsertFallbackText` 경로 —
둘 다 순수 텍스트 붙여넣기 무회귀, 이미지 단독 붙여넣기(텍스트 불변+
이미지만 추가), 혼합 붙여넣기(텍스트 삽입 + 이미지 추가 동시),
상한 초과(5장 캡+에러 문구) 전부 확인. `compressImage` 경유 확인
(저장된 문자열이 원본 PNG가 아니라 항상 `data:image/jpeg;...`로
재인코딩됨 — 원본 base64 직저장이 아님을 실측 증명). 편집 모달은
기존 `imgs[]`(빈 배열 포함) 위에 정상 추가되고 썸네일 렌더까지 확인.
캡처 단계 우선권 자체도 별도 DOM 구조 테스트로 재확인(조상 요소의
capture 리스너에서 `stopPropagation`하면 하위 타깃 요소 자신의
리스너가 아예 안 불림 — Tiptap의 `.ProseMirror` 자체 핸들러를 항상
막을 수 있다는 전제의 근거). `node --check` 전체 js 통과.

**범위 밖(지시대로 보고만)**: iOS 사파리 길게눌러 붙여넣기 — 이
환경은 데스크톱 헤드리스 Chromium뿐이라 실기기 터치 붙여넣기
제스처는 직접 재현 불가. 코드 레벨로는 표준 `paste` 이벤트(`
clipboardData.files`)만 사용해 iOS Safari 자체는 이 이벤트를 길게눌러
붙여넣기 시에도 지원하는 것으로 알려져 있으나(사파리 공유 시트가
아니라 표준 붙여넣기 메뉴 경유), **실기기 최종 확인은 사용자 몫**.
iOS 공유 시트에서 앱으로 직행하는 것은 별개 기능(PWA share target,
iOS 미지원)이라 R-09로 이미 분리돼 있어 이번 범위 아님.

- **B-123 완료·push 완료**(`23cbeb7`/`fd9fbf8`).
- **다음**: 사용자 실사용 피드백 대기 — 라이트박스·이미지 붙여넣기
  실기기(특히 iOS) 확인 결과에 따라 후속 조정 가능.

---

# 이전 핸드오프 — B-124+B-125 완료 (2026-07-20) 액션 드래그 분류 오변경 버그 + 수집함 피드 폭

## 최신 작업: 실사용 피드백 2건 — 드래그 정렬 버그 수정 + 리스트뷰 폭 상향

```
d0b27c0 fix: 드래그 재정렬 시 빈 칼럼 리빌 오버슈트로 인한 분류 오변경 (B-124)
1aa0ab5 style: 수집함 리스트뷰(피드) 대형 모니터 폭 상향 (B-125)
```

커맨드센터 발급 지시서로 착수. 손 B 부재로 파일 락 제약 없이 진행,
지시대로 2커밋 분리.

**B-124 원인**(`actions.js` `bindActDrag`): B-114가 도입한
`.act-live-groups.is-dragging .act-group.is-empty{display:block}`
(style.css) — 항목이 0개라 평소엔 숨겨진 칼럼(예: '계약')이 드래그를
"시작하는 순간" 나타나며 레이아웃이 밀리는 게 근본 원인. 사용자가
자기 칼럼('매물준비') 맨 아래에서 순서만 바꾸려고 살짝 오버슈트하면,
방금 새로 나타난 옆 빈 칼럼의 본문이 바로 그 자리를 차지하고 있어
드롭이 거기로 잡혀 분류가 바뀜 — 사용자 입장에선 "같은 칼럼 안에서
순서만 바꿨는데" 분류가 바뀌는 것처럼 보임. Playwright로 실제
마우스다운→이동→업 드래그를 재현해 좌표를 스윕하며 정확한 지점(빈
칼럼 헤더 통과 후 본문 진입 시점)에서 재현 확인, 헤더 자체는
리스너가 없어 이미 안전지대였다는 것도 확인.

**경계 판정 규칙**(신규 확정, 보고): ① 같은 칼럼 본문 안(행 위/빈
여백 모두) 드롭 → 분류 불변, 순서만 재계산(기존 동작 그대로). ②
드래그 시작 전부터 항목이 있던 다른 칼럼 본문에 드롭 → 분류 변경
허용(B-114 의도한 재분류 기능, 그대로 유지). ③ 칼럼-칼럼 사이
gap(10~12px)에 드롭 → 어떤 리스너도 없어 조용히 무시(기존에도
안전, 변경 없음). ④ **드래그 시작 시점엔 비어 있던 칼럼**의 본문에
드롭 → 이번에 신규 차단(분류 불변, 조용히 무시) — 헤더와 동일하게
"드롭 불가 지대"로 취급. 완전히 빈 칼럼으로 옮기려면 기존에도 있던
수정 모달의 분류 드롭다운 사용(대체 경로 이미 존재해 기능 손실
아님). 구현은 `body.closest('.act-group').classList.contains
('is-empty')` 체크 하나 — `is-empty` 클래스는 상태 불변인 드래그
도중엔 바뀌지 않아(재렌더는 드롭 후에만 발생) 별도 스냅샷 없이
드롭·dragover 핸들러에서 매번 즉시 확인 가능.

**검증**(Playwright, 네이티브 마우스 드래그 시뮬 — 합성 이벤트가
아니라 실제 mousedown/move/up으로 브라우저 자체 HTML5 DnD 엔진 경유):
①번 재현 버그 케이스(자기 칼럼 맨 아래 빈 '계약' 칼럼 본문까지
오버슈트) — 수정 전 분류가 '계약'으로 잘못 바뀜을 먼저 확인 후,
수정 후 분류 불변 확인(PASS). 기존 같은-칼럼 재정렬(합성 이벤트 +
네이티브 마우스 양쪽, 1000/1300/1440px 3개 폭) 무회귀. **이미
항목이 있던 칼럼으로의 정당한 재분류**(행 중앙 조준) 여전히 정상
동작 확인(수정으로 기능 손실 없음 재확인). **모바일 ▲▼ 버튼**
(390px, 드래그 핸들 자체가 숨겨지는 폭)은 애초에 `actMoveByButton`이
같은 그룹 내에서만 타깃을 찾아 분류 변경 경로 자체가 없음을 코드
검토+실측 재확인, 무회귀. `node --check` 전체 js 통과.

**B-125**(`style.css`): 수집함 리스트뷰(`#panel-scraps .sc-list`)
`max-width:880px` 중앙 정렬 고정이 1920/2560 같은 대형 모니터에서
카드 오른쪽에 거대한 빈 여백을 남기는 문제. `@media(min-width:1440px)`
블록(기존 갤러리 그리드 규칙이 있던 자리)에 `max-width:1180px`
override 1줄 추가 — 1439px 이하(모바일 포함)는 기존 880px 그대로
(계산된 `getComputedStyle` 값으로 실측 확인, 무변화). 1100~1240
후보 중 1180 선택 근거: Playwright로 1440/1920/2560 3개 폭에서
880(기존)·1180·1240 스크린샷 비교 — 1240은 1440px 프레임에서
좌우 여백이 거의 없어 최소 폭 끝에서 답답함(가독 한계에 더 가까움),
1180은 1440에서 여백이 자연스럽고 1920/2560에서도 본문 줄 길이가
과도하게 길어지지 않아 균형점으로 판단. 리스트 카드의 마크다운
렌더·"원문 보기" 토글·"더보기" 펼침·XSS(`<script>` payload 무실행)
전부 폭 변경 후에도 정상 동작 확인, 콘솔 에러 0건.

- **B-124/B-125 완료·push 완료**(`d0b27c0`/`1aa0ab5`).
- **다음**: 인스타 레퍼런스 강화(B-123, 카드뷰 이미지 중심+클립보드
  붙여넣기 수집) 착수 대기 — BACKLOG상 "B-124/125 후 착수"로 배정됨.

---

# 이전 핸드오프 — B-42 완료 (2026-07-20) 가격 타임라인 — CRM 심화층 종료

## 최신 작업: history[] 기반 추세 배지+스파크라인 (표시 전용)

```
33cbe2e feat: 가격 타임라인 — history[] 기반 추세 표시 (B-42)
```

커맨드센터 확정 스펙으로 착수. B-43의 `listings[].history[]`를
읽기만 하는 순수 표시 기능 — 저장·판정·정렬 개입 없음, 스키마
무변경. 손 B 부재로 파일 락 제약 없이 진행, 지시대로 1커밋.

**추세 배지**(`depositTrendBadge(l)`, `properties.js`): 매물 행
(`renderCxListings`)과 단지 카드 헤드라인(`renderComplexes`의
`c-price`, 대표매물 기준) 양쪽에 삽입. `history`에서 `deposit`이
있는 항목만 걸러 **가장 최근 인접 2건**을 비교 — 반올림
(`Math.round(diff*100)/100`) 후 0이면 무표시(부동소수점 오차로
"↓0억" 같은 거짓 배지가 뜨는 것 방지, 또한 상태만 바뀌고 가격은
그대로인 edit 케이스도 자연히 무표시됨). 하락은 `.chip.ok`(긍정,
초록 계열), 상승은 `.chip.warn`(주의, 주황 계열) — 둘 다 기존
클래스 재사용, 새 색상 0개. 이력 2건 미만이면 무표시(소급·추정
금지 원칙 그대로 — 데이터가 막 쌓이기 시작한 지금은 대부분의
매물이 아직 무표시인 게 정상).

**스파크라인**(`listingSparklineHTML(l)`, `properties.js`): B-117
상세 패널 "변동 이력" 리스트 바로 위에 삽입. 외부 차트 라이브러리
없이 인라인 SVG를 직접 문자열로 생성 — `viewBox="0 0 240 40"` 고정,
`deposit` 값들을 min/max로 정규화해 점(circle)+꺾은선(path) 좌표
계산. 전 구간 값이 동일하면 `range=max-min||1`로 0 나눗셈을
피해 가운데 일직선을 그림(크래시 대신 안전한 폴백). 좌표는 전부
`Math.round`/`toFixed` 결과인 숫자 문자열이라 사용자 텍스트가 섞일
경로가 원천적으로 없음 — `priceHTML`의 `${rep.deposit}`처럼 기존
코드가 순수 숫자 보간에 `esc()`를 안 쓰는 관례와 동일하게 `esc()`
생략(테스트로 실제 주입 불가능함을 재확인, 아래 검증 참고). CSS는
`.cx-listing-spark{width:100%;height:40px}` 1줄만 추가 — 뷰박스로
내부 좌표는 고정하고 실제 렌더 폭만 반응형.

**검증**(Playwright, 40개 체크, 데스크톱 1440×900+모바일 390×844):
이력 0/1/2(하락)/2(상승)/5건(혼합, 마지막 두 값이 우연히 같은
케이스 포함) 5개 매물 fixture로 — 0·1건 무표시, 2건 하락 "↓0.2억"
정확 표시(클래스 `chip ok` 확인), 2건 상승 "↑0.1억" 정확 표시
(클래스 `chip warn` 확인), 5건이어도 마지막 두 값이 같으면 무표시
확인(단순 "이력 개수"가 아니라 "값이 다른지"가 기준임을 실측).
단지 카드 헤드라인에도 동일 배지 노출 확인. 스파크라인은 2건
이상일 때만 SVG 1개 렌더, `path` `d` 속성이 `M`/`L` 명령+숫자만
포함하는 정규식으로 주입 가능성 재확인(`circle` 개수가 이력
개수와 정확히 일치), 전 구간 동일값(0,0) 입력 시 NaN 없이 안전
렌더 확인. **B-43 변동 이력 리스트·B-27-lite 안전체크 무회귀**
(스파크라인과 같은 패널에 공존해도 서로 안 깨짐). **Redis 왕복**
— POST 바디의 listing 객체 키에 `trend`/`spark` 등 새 필드가
전혀 없고 `history`만 그대로임을 확인해 "스키마 무변경(읽기
전용)" 스펙을 실측으로 재확인. `node --check` 13개 js 파일 전부
통과. 스크래치 테스트 스크립트는 실행 후 삭제.

- **B-42 완료·push 완료**(`33cbe2e`).
- **다음**: CRM 심화층(B-41·B-43·B-42) 전부 소진 — 사용자 별도
  지시 대기. BACKLOG.md상 다음 후보는 B-05(레거시 삭제, 손 B
  복귀 7/25 후) 또는 신규 실사용 피드백.
