# HANDOFF — B-71(진단: 변경불요) + B-20 완료 (2026-07-18)

## ⚠️ 확인 필요: B-71은 코드 변경 없이 완료
지시서는 "수집함 편집 모달에 유형(type) 변경 추가"를 요청했지만, 착수
전 진단 결과 **이미 완전히 구현돼 있었습니다**(`sem_typeChips`, 최초
모듈 분리 리팩터 시점부터 존재). 상세는 아래 2번 참고. BACKLOG.md의
B-71 설명("현재는 추가 시에만 선택 가능")을 정정 요청드립니다.

## 1. 목표
`BACKLOG.md` ⭐ 섹션 B-71(수집함 편집 모달 유형 변경) + B-20(수동 매물
추가 중복 가드), 한 지시서·커밋 분리 예정. 손 B가 `auth.js`·
`assets.js`·`style.css`로 B-79·B-73 작업 중이라 해당 파일 무접촉.

## 2. 완료
**커밋 2개(진단 문서 1 + 코드 1), 검증 완료. push는 검증 후 진행 가능.**

```
86dcd40 fix: 수동 매물 추가 중복 확인 (B-20)
b3fc2f4 docs: B-71 진단·B-20 HISTORY 기록
```

**B-71 — 진단 결과 코드 변경 없음.** 착수 전 코드 확인 결과:
- `index.html:650` `sem_typeChips`가 추가폼(`sc_typeChips`)과 동일한
  8개 유형 칩으로 이미 존재.
- `js/scraps-form.js`의 `semUpdatePropFields()`가 `scUpdatePropFields()`
  와 완전히 동일한 구조로 이미 존재 — 모달 오픈 시점·칩 클릭 시점
  양쪽에서 호출돼 SC_PROPLESS(note·ai_log) ↔ 매물성 전환 시
  `sem_fieldPrice`/`sem_rowAreaSchedule` 표시/숨김 정확히 동작.
- 저장 핸들러가 선택된 칩의 `dataset.type`을 정확히 읽어 저장.
- `git log -S`로 확인: `b1862f0`(최초 모듈 분리) 시점부터 존재 —
  최근 추가가 아니라 애초부터 있던 기능.
- Playwright로 재확인(데스크톱+모바일): 매매→메모 전환 시 가격·면적
  필드는 숨겨지지만 입력값은 DOM에 그대로 남고, 저장 후 매매로 되돌려도
  값이 정확히 복원됨(지시서의 "데이터 보존" 요구사항도 이미 충족).

지시서가 요청한 "SC_TYPE select 추가"를 그대로 만들었다면 이미 있는
칩 UI와 중복·충돌하는 새 컨트롤이 생겨 오히려 UX가 나빠졌을 것 —
변경하지 않는 것이 맞다고 판단해 코드 변경 없이 진단만 커밋했습니다.

**B-20(코드 변경)** — `saveAsComplexListing()`에 임포트 경로가 이미
쓰는 `listingExists()` 호출 추가. 자동 차단이 아니라
`confirm('같은 동호수·보증금 매물이 있어요. 그래도 추가할까요?')`로
확인만, 사용자가 강행 추가 가능(취소 시 B-19확과 동일하게 폼 유지·
입력값 보존). 새 중복판정 로직 없이 기존 `listingExists()` 재사용뿐.

- **검증**(Playwright, 데스크톱 1280px+모바일 390px):
  - B-71: 유형 전환 왕복(sale→note→sale) 후 가격·면적·일정 데이터
    완전 보존 확인(데스크톱+모바일).
  - B-20: 신규(중복 없음) → 확인창 없이 바로 추가. 완전 동일한
    동호수·면적·보증금 재입력 → 지시서 그대로의 문구로 확인창 노출,
    취소 시 저장 중단+폼 유지, 확인 시 중복임에도 강행 추가됨. 동호수만
    다른 경우 → 확인창 없이 바로 추가(무회귀).
  - `node --check js/properties.js` 통과, `properties.js` 1파일만
    코드 diff(+10/-3줄).
  - 로컬 python 테스트 서버·Playwright 스크립트는 레포 바깥
    scratchpad에서만 실행, 세션 종료 전 전부 삭제.

## 3. 미완 / 다음 단계
- **B-71 완료(변경 없음)** — BACKLOG.md ⭐ 섹션에서 커맨드센터가
  "이미 구현됨" 확인 후 삭제 처리할 차례. 설명 정정 참고 바람.
- **B-20 완료** — BACKLOG.md ⭐ 섹션에서 삭제 처리할 차례.
- push는 이 세션에서 검증 완료 후 진행(보류 아님, 지시대로).

## 4. 주의점
- PIN·API 키·실제 데이터는 이 문서 어디에도 기록 안 함.
- **BACKLOG.md는 커맨드센터 소유(읽기 전용)** — 이번 세션에서도 전혀
  수정·커밋 안 함.
- **`auth.js`·`assets.js`·`style.css` 무접촉 준수** — 착수 전 `git
  status`로 확인, 손 B가 B-79(`d53872a`)·B-73(`149e744`)로 해당
  파일들을 작업 중이었으나 커밋 시점마다 확인해 항상 내 파일만
  스테이징. `HISTORY.md` 공유 로그도 Codex의 B-73 항목을 그대로 두고
  내 항목만 이어붙이는 방식으로 추가(삭제·덮어쓰기 없음, `git add`
  전 매번 diff 확인해 Codex 몫이 섞이지 않게 함).
- 스키마 변경 없음. 새 CSS 클래스·새 JS 파일 없음.

## 5. 컨텍스트
- 이 레포는 "머리(커맨드센터, 코드 미수정) — 손 A(Claude Code) — 손
  B(Codex)" 3자 협업 구조. SSOT는 `CLAUDE.md`, 실행 규칙은
  `AGENTS.md`, 이력은 `HISTORY.md`, 백로그는 `BACKLOG.md`(커맨드센터
  전용).
- 직전 세션들: ... → B-68+B-19확(단지 매칭 제안) → B-79(손 B, 게스트
  담당자 표시 복원, `d53872a`) → B-73(손 B, 자산 테이블 모바일 개선,
  `149e744`) → **이번 세션(B-71 진단+B-20, 검증 완료) — 유형 변경은
  이미 있었음 확인, 매물 중복 확인 안내 추가**. B-79·B-73과 파일
  무충돌 병렬 진행(`properties.js` vs `auth.js`/`assets.js`/`style.css`).
